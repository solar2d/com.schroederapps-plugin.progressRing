Sources for the plugin `plugin.progressRing`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.progressRing" = {
            publisherId = "com.schroederapps",
        },
    },
}
```
